<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:TableConverter.ViewModels.Tools"
             xmlns:sukiExtensions="clr-namespace:SukiUI.Theme;assembly=SukiUI"
             xmlns:controls="clr-namespace:TableConverter.Views.Controls"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="TableConverter.Views.Tools.TableUtilitiesView"
             x:DataType="viewModels:TableUtilitiesViewModel">
    <Grid RowDefinitions="Auto,Auto"
          RowSpacing="10">
        <Grid Grid.Row="0"
              ColumnSpacing="5"
              IsSharedSizeScope="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"
                                  SharedSizeGroup="Numerics" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto"
                                  SharedSizeGroup="Numerics" />
            </Grid.ColumnDefinitions>
            <NumericUpDown Grid.Column="0"
                           Value="{Binding HeadersCount, Mode=TwoWay}"
                           ParsingNumberStyle="Integer"
                           HorizontalAlignment="Stretch"
                           sukiExtensions:NumericUpDownExtensions.Unit="Columns">
            </NumericUpDown>
            <TextBlock Grid.Column="1" 
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center">
                X
            </TextBlock>
            <NumericUpDown Grid.Column="2"
                           Value="{Binding RowCount, Mode=TwoWay}"
                           ParsingNumberStyle="Integer"
                           HorizontalAlignment="Stretch"
                           sukiExtensions:NumericUpDownExtensions.Unit="Rows">
            </NumericUpDown>
        </Grid>
        <controls:CommandButtonToolBar Grid.Row="1"
                                       ItemsSource="{Binding GeneralCommands, Mode=OneWay}">
            <controls:CommandButtonToolBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid RowSpacing="10" 
                                 Columns="2" />
                </ItemsPanelTemplate>
            </controls:CommandButtonToolBar.ItemsPanel>
        </controls:CommandButtonToolBar>
    </Grid>
</UserControl>
